# Install PyTorch first, matched to your CUDA version:
#   pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 \
#       --index-url https://download.pytorch.org/whl/cu118

# Diffusion stack. These three are pinned together: the anchor-aware UNet is
# built against the diffusers 0.19 model API, which later releases changed.
diffusers==0.19.0
transformers==4.31.0
accelerate==0.21.0
xformers==0.0.22

# Geometry and mesh export
plyfile==1.0.1
scikit-image==0.19.3

# Image and video IO
opencv-python==4.6.0.66
imageio-ffmpeg==0.4.8

# Metrics
lpips==0.1.4

# Configuration, logging and utilities
ConfigArgParse==1.7
mmengine==0.10.4
einops==0.6.1
jaxtyping==0.2.19
rich==13.4.2
tensorboard==2.13.0
tqdm==4.65.0
