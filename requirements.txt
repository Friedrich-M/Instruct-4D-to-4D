# Install PyTorch first, matched to your CUDA build. Use 2.13 or newer: earlier
# releases carry known advisories. Pick the right wheel index from
# https://pytorch.org/get-started/locally/
#   pip install "torch>=2.13" torchvision

# Diffusion stack. The pseudo-3D UNet reaches into diffusers internals that have
# moved between releases; instruct4d/ip2p/_compat.py bridges those, and the code
# is verified against diffusers 0.19 through 0.40.
diffusers>=0.38.0
transformers>=5.10.0
accelerate>=1.14.0

# Geometry and mesh export
plyfile>=1.0.1
scikit-image>=0.19.3

# Image and video IO
opencv-python>=4.8.1.78
imageio-ffmpeg>=0.4.8

# Metrics
lpips>=0.1.4

# Configuration, logging and utilities
ConfigArgParse>=1.7
mmengine>=0.10.4
einops>=0.6.1
jaxtyping>=0.2.19
rich>=13.4.2
tensorboard>=2.13.0
tqdm>=4.66.3
